# Installing Devkit for OpenCode

Add to the `plugin` array in your `opencode.json`:

```json
{
  "plugin": ["devkit@git+https://github.com/rahulmutt/devkit.git"]
}
```

Restart OpenCode. Verify by asking: "Tell me about your devkit skills."
