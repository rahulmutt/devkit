repos:
  - repo: local
    hooks:
      - id: devkit-release-gate
        name: devkit release gate (fmt-check, lint, typecheck, generated files in sync, tests)
        entry: mise run release
        language: system
        pass_filenames: false
